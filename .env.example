# 認証情報（必ず .env にコピーして値を設定すること）
# .env は .gitignore に含まれるためコミットされません。

# ログイン用メールアドレス
GOLF_RESERVATION_EMAIL=your-email@example.com

# ログイン用パスワード
GOLF_RESERVATION_PASSWORD=your-password

# Slack 起動通知（任意）。設定するとスクリプト実行開始時に Slack へ投稿
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/xxx/xxx

# 予約してよい日（任意）。Slack の候補日を見て、この日だけ予約したいときに指定。カンマ区切り。
# 例: ALLOWED_DAYS=2,5,9  → config.yaml の preferredDays より優先
# ALLOWED_DAYS=2,5,9
